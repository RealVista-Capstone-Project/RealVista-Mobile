/**
 * Notification Badge Widget
 * Displays unread notification count on tab bar or other UI elements
 */
export function NotificationBadge() {
  // UI-only for now: backend notification APIs are not implemented yet.
  // Keep this component as a placeholder; re-enable useUnreadCount() when API is ready.
  return null
}
